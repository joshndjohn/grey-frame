import styled from "styled-components";

export const LinkStyle = styled.a`
    text-transform: uppercase;
    letter-spacing: 1px;
    font-weight: 400;
    font-size: 14px;
    transition: color .5s;
`